{
  "_comment": "使い方: このファイルを config.defaults.local.json としてコピーし、実値を入れる。local.json は .gitignore 済み。コミットされない。このファイル（example）には実値を書かない。gasWebAppUrl/gasToken は v3.0.0 で廃止。",
  "geminiApiKey": "YOUR_GEMINI_API_KEY_HERE"
}
