{
  "name": "quick-calendar",
  "version": "2.6.0",
  "description": "Quick-calendar — テキスト選択でGoogleカレンダーに即登録",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "gas:push": "bash ./gas/deploy-gas.sh"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "jest": "^29.7.0",
    "@testing-library/jest-dom": "^6.1.4"
  }
}
